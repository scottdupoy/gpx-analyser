CXX = g++
CPPFLAGS = -std=c++11 -MMD
LFLAGS := -L/usr/local/lib
OBJ_DIR = obj
BIN_DIR = bin
SRC_DIR = src
INCLUDE_DIR = include
INCLUDES = -I/usr/local/include

ifeq ($(OS),Windows_NT)
DLL_PREFIX = 
DLL_SUFFIX = .dll
else
DLL_PREFIX = lib
DLL_SUFFIX = .so
endif

